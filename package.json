{
  "name": "electron-clone",
  "private": true,
  "scripts": {
    "dev": "node server.js",
    "wrangler": "cd worker/clone-dev && npx wrangler dev --port 8787",
    "ui": "cd web && npx vite --port 8834 --host"
  },
  "dependencies": {
    "redis": "^4.6.0"
  }
}
